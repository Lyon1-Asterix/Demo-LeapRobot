 //HandList hands = leap.frame().hands();
    //Hand firstHand = hands.get(0);
    //if (firstHand.isValid()) {
      //println("is Valid");  
     // println ("pitch : " + firstHand.direction().pitch());
      //println("yaw : " + firstHand.direction().yaw());
      //println ("roll : " + firstHand.palmNormal().roll());
      //println("");
      //if (firstHand.direction().pitch() > 0.5) {
      //   port.write(1); 
      //} else if (firstHand.direction().pitch() < 0.5) {
      //  port.write(2);
      //} else {
      //   port.write(-1); 
      //}